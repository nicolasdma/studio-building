import { Sparkles } from "@react-three/drei";

export default function Effects() {
  return (
    <Sparkles
      count={50}
      size={4}
      scale={30}
      color="#68ffd1"
      position-y={2}
    />
  );
}
